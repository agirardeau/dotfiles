alias la='ls -A'
alias ll='ls -l'
alias lla='ls -lA'

alias tmux='tmux -2'

alias findr='find . -name'

alias grepr='grep -rni . -e'
alias greprs='grep -rn . -e'

alias psfind='ps -A | grep'

# customtree() {
#     tree "$@"
#     eval "$(dircolors ~/.dircolors)"
# }

