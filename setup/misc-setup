# Currently no miscellaneous setup to do that isn't environment specific.

if grep -q Microsoft /proc/version; then
    echo "Running on WSL. Consider running setup/windows/misc-setup-windows (with"
    echo "appropriate changes to path)."
fi
